{
  "name": "server",
  "version": "1.0.0",
  "description": "Currently, the API has no endpoints. They will be implemented in the next milestone",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
